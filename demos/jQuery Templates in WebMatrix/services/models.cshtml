@{
    Layout = "";

    var model = Request["Model"];
    
    List<Car> cars = new List<Car>();

    for( int i = 0; i < 10; i++ ) {
        cars.Add( new Car() { Model = model, Year = "200" + i.ToString() } );    
    } 
    
    Response.ContentType = "application/json";
    Response.Write(Json.Encode(cars));
    
}